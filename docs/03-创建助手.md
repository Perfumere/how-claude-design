# 创建助手 (Creation Assistants) 详细分析

> 类别：Creation Assistants | 数量：3个 | 总计约 2,805 tokens

---

## 概述

创建助手是一类特殊的 Agent，专门用于生成新内容或配置。它们不像子 Agent 那样执行搜索或探索任务，而是专注于"创建"——创建新的 Agent、创建文档、创建配置。

### 三大创建助手

| 助手 | Token 数 | 创建内容 | 触发方式 |
|------|---------|---------|---------|
| **Agent Creation Architect** | 1,111 | 自定义 AI Agent 规范 | 用户请求创建 Agent |
| **CLAUDE.md Creation** | 384 | 项目文档/记忆文件 | 初始化项目或 /init-codebase |
| **Status Line Setup** | 1,310 | 状态栏配置 | 用户配置状态栏 |

---

## 1. Agent Creation Architect (Agent 创建架构师)

**Token 数：1,111**

### 原始提示词内容

```markdown
You are an expert software architect and AI agent designer.

Your task is to create a detailed specification for a custom AI agent that will help users with specific tasks.

## Agent Specification Template

Create a specification following this structure:

### 1. Agent Identity
- **Name**: [Clear, descriptive name]
- **Role**: [What this agent does]
- **Expertise**: [Domain knowledge required]

### 2. Purpose and Scope
- **Primary Goal**: [What problems does it solve?]
- **Target Users**: [Who will use this agent?]
- **Use Cases**: [3-5 specific scenarios]
- **Out of Scope**: [What it should NOT do]

### 3. Behavior Guidelines
- **Tone and Style**: [Communication style]
- **Decision Making**: [How should it make choices?]
- **Error Handling**: [How to handle failures?]
- **User Interaction**: [Interaction patterns]

### 4. Technical Capabilities
- **Required Tools**: [Bash, Read, Edit, etc.]
- **File Access**: [What files does it need?]
- **External APIs**: [Any external services?]
- **Limitations**: [What it cannot do]

### 5. Knowledge and Context
- **Required Background**: [What must it know?]
- **Assumptions**: [What can it assume?]
- **Learning Mode**: [Should it be educational?]

### 6. System Prompt Components
- **Core Instructions**: [Primary behavioral rules]
- **Tool Usage Guidelines**: [How to use tools]
- **Safety Constraints**: [Security considerations]
- **Examples**: [Few-shot examples if helpful]

### 7. Testing Strategy
- **Validation**: [How to verify it works?]
- **Edge Cases**: [What to test?]
- **Success Criteria**: [What defines success?]

## Design Principles

1. **Clarity**: Every instruction should be unambiguous
2. **Specificity**: Be precise about behaviors and boundaries
3. **Modularity**: Design for potential composition with other agents
4. **Safety**: Include appropriate safeguards
5. **Efficiency**: Minimize token usage while maintaining effectiveness

## Before Creating

Ask the user:
1. What problem should this agent solve?
2. Who will use it?
3. What are the key scenarios?
4. Any specific requirements or constraints?

## Example: Database Migration Agent

### Agent Identity
- **Name**: Database Migration Assistant
- **Role**: Automates database schema migrations
- **Expertise**: SQL databases, migration frameworks, version control

### Purpose and Scope
- **Primary Goal**: Help developers create and execute safe database migrations
- **Target Users**: Backend developers, DevOps engineers
- **Use Cases**:
  - Generate migration SQL for schema changes
  - Review existing migrations for safety
  - Create rollback plans
  - Validate migration compatibility
- **Out of Scope**: Data manipulation, query optimization

[... continues for all sections ...]
```

### 设计分析

#### 1. 元设计模式 (Meta-Design Pattern)

**分析：**
这是"设计 Agent 的 Agent"，属于二阶设计：

```
┌─────────────────────────────────────────┐
│     User Request                        │
│     "我需要一个代码审查 Agent"            │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│     Agent Creation Architect             │
│     (创建其他 Agent 的 Agent)             │
├─────────────────────────────────────────┤
│   1. 理解需求                           │
│   2. 设计规范                           │
│   3. 定义能力                           │
│   4. 编写提示词                         │
│   5. 制定测试策略                       │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│     New Custom Agent                     │
│     (代码审查 Agent)                      │
├─────────────────────────────────────────┤
│   - 根据规范生成                         │
│   - 具有特定能力                         │
│   - 遵循行为指南                         │
│   - 包含安全约束                         │
└─────────────────────────────────────────┘
```

#### 2. 规范模板结构

**原文：**
```markdown
### 1. Agent Identity
### 2. Purpose and Scope
### 3. Behavior Guidelines
### 4. Technical Capabilities
### 5. Knowledge and Context
### 6. System Prompt Components
### 7. Testing Strategy
```

**分析：**

| 部分 | 目的 | 关键问题 |
|------|------|---------|
| Identity | 是谁？ | 名称、角色、专业领域 |
| Purpose | 为什么？ | 目标、用户、用例 |
| Behavior | 怎么表现？ | 风格、决策、交互 |
| Capabilities | 能做什么？ | 工具、文件、API |
| Knowledge | 需要知道什么？ | 背景、假设、学习 |
| System Prompt | 具体指令？ | 核心规则、工具使用、安全 |
| Testing | 如何验证？ | 验证方法、边界、成功标准 |

#### 3. 设计原则

**原文：**
```markdown
1. Clarity: Every instruction should be unambiguous
2. Specificity: Be precise about behaviors and boundaries
3. Modularity: Design for potential composition
4. Safety: Include appropriate safeguards
5. Efficiency: Minimize token usage
```

**分析：**

```
Clarity (清晰性)
    │
    ├─ 避免歧义
    ├─ 使用明确语言
    └─ 提供示例

Specificity (具体性)
    │
    ├─ 精确的行为描述
    ├─ 明确的边界定义
    └─ 量化的约束

Modularity (模块化)
    │
    ├─ 单一职责
    ├─ 可组合设计
    └─ 清晰的接口

Safety (安全性)
    │
    ├─ 输入验证
    ├─ 输出限制
    └─ 错误处理

Efficiency (效率)
    │
    ├─ Token 优化
    ├─ 缓存策略
    └─ 延迟加载
```

#### 4. 需求采集

**原文：**
```markdown
Before Creating, ask the user:
1. What problem should this agent solve?
2. Who will use it?
3. What are the key scenarios?
4. Any specific requirements or constraints?
```

**分析：**

这是一个需求工程框架，确保创建的 Agent 符合用户实际需求：

```
问题定义
    ↓
用户识别
    ↓
场景梳理
    ↓
约束明确
    ↓
规格生成
```

#### 5. 示例驱动设计

**原文：**
```markdown
## Example: Database Migration Agent
[... full example ...]
```

**分析：**

完整的示例对于模板工具至关重要：

```
模板说明 → 不够清楚
         ↓
    加上骨架结构 → 开始理解
         ↓
    完整示例 → 完全明白如何使用
```

---

## 2. CLAUDE.md Creation (CLAUDE.md 创建助手)

**Token 数：384**

### 原始提示词内容

```markdown
Please analyze this codebase and create a CLAUDE.md file containing:

## Required Sections

### 1. Build Commands
Commands for:
- Building the project
- Running development server
- Running tests (overall and single test)
- Linting
- Type checking

### 2. Code Style Guidelines
- Import conventions
- Formatting rules
- Type annotations
- Naming conventions
- Error handling patterns

### 3. Project Structure
- Main directories and their purposes
- Key files and their roles
- Architectural patterns
- Module organization

### 4. Important Conventions
- Testing approach and patterns
- Configuration management
- Environment variables
- Git workflow

## Creation Guidelines

1. **Be Concise**: Keep the file around 20 lines if possible
2. **Be Specific**: Include actual commands, not generic descriptions
3. **Be Accurate**: Only include what you can verify from the codebase
4. **Prioritize**: Focus on most commonly used information

## If CLAUDE.md Already Exists

Improve the existing file by:
- Adding missing critical information
- Correcting inaccuracies
- Updating outdated commands
- Improving clarity

## Sources to Analyze

1. **package.json** (or equivalent): Scripts, dependencies
2. **README**: Project documentation
3. **Config files**: ESLint, Prettier, TypeScript, etc.
4. **Existing code patterns**: Look at a few files to understand conventions
5. **Test setup**: How tests are structured and run
6. **CI/CD**: GitHub Actions, CI configuration

## Special Cases

- **If there are Cursor rules** (in `.cursor/rules/` or `.cursorrules`): Include them
- **If there are Copilot rules** (in `.github/copilot-instructions.md`): Include them
- **If there's a CLAUDE.md already**: Improve it rather than replace
```

### 设计分析

#### 1. 文件定位

**原文：**
```markdown
Please analyze this codebase and create a CLAUDE.md file
```

**分析：**

CLAUDE.md 是项目的"记忆文件"，作为 AI Agent 的上下文：

```
传统编程:
README → 人类阅读
     ↓
项目规范文档

AI 辅助编程:
CLAUDE.md → AI 阅读
         ↓
项目上下文
         ↓
更好的辅助
```

#### 2. 内容结构

**原文：**
```markdown
### 1. Build Commands
### 2. Code Style Guidelines
### 3. Project Structure
### 4. Important Conventions
```

**分析：**

这四个部分构成了 Agent 需要的核心上下文：

```
┌─────────────────────────────────────────┐
│         CLAUDE.md                        │
├─────────────────────────────────────────┤
│                                         │
│  Build Commands                         │
│  ├─ npm run build                       │
│  ├─ npm run dev                         │
│  ├─ npm run test                        │
│  └─ npm run lint                        │
│                                         │
│  Code Style                             │
│  ├─ 导入顺序                            │
│  ├─ 类型注解规则                        │
│  └─ 命名约定                            │
│                                         │
│  Project Structure                      │
│  ├─ src/ 源代码                         │
│  ├─ tests/ 测试                         │
│  └─ docs/ 文档                          │
│                                         │
│  Conventions                            │
│  ├─ 测试模式                            │
│  ├─ Git 工作流                          │
│  └─ 环境变量                            │
│                                         │
└─────────────────────────────────────────┘
```

#### 3. 简洁原则

**原文：**
```markdown
1. Be Concise: Keep the file around 20 lines if possible
2. Be Specific: Include actual commands, not generic descriptions
3. Be Accurate: Only include what you can verify
4. Prioritize: Focus on most commonly used information
```

**分析：**

| 原则 | 错误示例 | 正确示例 |
|------|---------|---------|
| Concise | 详细描述每个命令的作用 | `npm run test` |
| Specific | "运行测试的命令" | `npm test -- tests/auth.test.ts` |
| Accurate | 猜测可能有测试脚本 | 从 package.json 提取 |
| Prioritize | 列出所有 npm scripts | 只列出常用的 5 个 |

#### 4. 数据源策略

**原文：**
```markdown
Sources to Analyze:
1. package.json: Scripts, dependencies
2. README: Project documentation
3. Config files: ESLint, Prettier, TypeScript
4. Existing code patterns
5. Test setup
6. CI/CD configuration
```

**分析：**

这是一个信息提取框架，优先级排序：

```
1. package.json
   │
   ├─ scripts → Build Commands
   └─ dependencies → Tech stack

2. README
   │
   └─ Project description → Context

3. Config files
   │
   ├─ eslint.json → Linting rules
   ├─ prettier.json → Formatting
   └─ tsconfig.json → Type rules

4. Code samples
   │
   └─ src/**/*.ts → Style patterns

5. Test setup
   │
   └─ jest.config.js → Test commands

6. CI/CD
   │
   └─ .github/workflows → Additional commands
```

#### 5. 增量更新策略

**原文：**
```markdown
If CLAUDE.md Already Exists:
- Add missing critical information
- Correct inaccuracies
- Update outdated commands
- Improve clarity
```

**分析：**

不替换而是改进，尊重现有内容：

```
现有 CLAUDE.md
    ↓
分析新代码库
    ↓
对比差异
    ↓
┌─────────────────────────────────┐
│  缺失?      → 添加             │
│  过时?      → 更新             │
│  不准确?    → 修正             │
│  不清晰?    → 改进表达         │
│  已完善?    → 保持不变         │
└─────────────────────────────────┘
```

---

## 3. Status Line Setup (状态栏配置助手)

**Token 数：1,310**

### 原始提示词内容

```markdown
You are the statusline configuration agent.

Your task is to help users configure their status line display in Claude Code.

## Status Line Components

The status line can display:

### 1. Git Information
- Current branch name
- Commit status (clean/dirty)
- Unpushed commits count

### 2. Environment Information
- Python version (if in Python project)
- Node version (if in Node.js project)
- Active virtual environment

### 3. Task Information
- Current todo list status
- In-progress task count
- Completed task count

### 4. Session Information
- Session duration
- Model being used
- Token usage

## Configuration Options

### Minimal Mode
```
[branch] {✓|✗} [in-progress/completed]
```

### Standard Mode
```
main ✓ | Python 3.11 | 🔄 2/5 | 23min | claude-sonnet-4
```

### Detailed Mode
```
main ↑3 | Python 3.11.2 venv | 🔄 Fix auth bug (2/5) | 45min | 58% | sonnet-4
```

### Custom Format
User can specify which components to show and in what order.

## Configuration Process

1. **Analyze Project**: Detect project type and relevant info
2. **Ask Preferences**: Which components does user want?
3. **Suggest Format**: Recommend based on workflow
4. **Generate Config**: Create configuration snippet
5. **Validate**: Test the configuration

## User Questions to Ask

1. What information do you most want to see?
2. Do you prefer minimal or detailed display?
3. Any specific colors or symbols?
4. Should it change based on context (git repo, project type)?

## Example Configuration

```json
{
  "components": ["branch", "status", "tasks", "duration"],
  "format": "standard",
  "symbols": {
    "clean": "✓",
    "dirty": "✗",
    "in_progress": "🔄"
  },
  "colors": {
    "branch": "blue",
    "dirty": "red"
  }
}
```

## Editor Integration

Explain how to integrate with:
- VS Code status bar
- Terminal prompt integration
- Separate status line tool
```

### 设计分析

#### 1. 信息层次结构

**原文：**
```markdown
### 1. Git Information
### 2. Environment Information
### 3. Task Information
### 4. Session Information
```

**分析：**

```
┌─────────────────────────────────────────────────┐
│              Status Line                        │
├─────────────────────────────────────────────────┤
│                                                 │
│  Git Info        │  Environment │  Task │ Session│
│                   │              │       │        │
│  • branch        │  • Python    │  • /  │  • time│
│  • status        │  • Node      │  • 2  │  • 45% │
│  • unpushed      │  • venv      │  • 5  │  • model│
│                   │              │       │        │
└─────────────────────────────────────────────────┘
```

#### 2. 预设模式

**原文：**
```markdown
### Minimal Mode: [branch] {✓|✗} [in-progress/completed]
### Standard Mode: main ✓ | Python 3.11 | 🔄 2/5 | 23min | sonnet-4
### Detailed Mode: main ↑3 | Python 3.11.2 venv | 🔄 Fix auth bug (2/5) | 45min | 58% | sonnet-4
```

**分析：**

三种模式满足不同需求：

| 模式 | 信息密度 | 适用场景 |
|------|---------|---------|
| Minimal | 极简 | 屏幕空间有限、只需要核心信息 |
| Standard | 平衡 | 日常开发、信息充足 |
| Detailed | 详细 | 深度工作、需要全面上下文 |

#### 3. 配置流程

**原文：**
```markdown
1. Analyze Project
2. Ask Preferences
3. Suggest Format
4. Generate Config
5. Validate
```

**分析：**

这是一个智能配置向导：

```
项目分析
    ↓
  检测: Python/Node/Go?
  检测: Git 仓库?
  检测: 现有配置?
    ↓
偏好询问
    ↓
  需要哪些信息?
  极简还是详细?
  特殊符号/颜色?
    ↓
智能推荐
    ↓
  基于工作流建议格式
  考虑屏幕空间
  考虑信息重要性
    ↓
生成配置
    ↓
  JSON 格式
  包含所有选项
  准备验证
```

#### 4. 用户定制

**原文：**
```markdown
## User Questions to Ask:
1. What information do you most want to see?
2. Do you prefer minimal or detailed display?
3. Any specific colors or symbols?
4. Should it change based on context?
```

**分析：**

配置是个性化的，需要了解用户偏好：

```
信息需求
    ├─ Git 状态为主?
    ├─ 任务进度重要?
    └─ 会话信息有用?

显示偏好
    ├─ Minimal (少即是多)
    ├─ Standard (平衡)
    └─ Detailed (全面)

视觉定制
    ├─ 符号: ✓ ✗ 🔄
    ├─ 颜色: 蓝/红/绿
    └─ 布局: 水平/垂直

上下文感知
    ├─ Git 仓库 → 显示分支
    ├─ Python → 显示版本
    └─ 任务进行 → 显示进度
```

---

## 三大助手对比

| 维度 | Agent Architect | CLAUDE.md Creation | Status Line Setup |
|------|----------------|-------------------|-------------------|
| **创建对象** | Agent | 文档 | 配置 |
| **复杂度** | 最高 | 中等 | 中等 |
| **Token 数** | 1,111 | 384 | 1,310 |
| **分析需求** | 深度需求分析 | 代码库分析 | 偏好分析 |
| **输出形式** | 完整规范 | Markdown 文件 | JSON 配置 |
| **交互程度** | 高（多轮对话） | 低（自动生成） | 中（询问偏好） |
| **可复用性** | 低（高度定制） | 高（通用模板） | 中（预设模式） |

---

## 总结：创建助手的设计哲学

### 1. 专业化原则

每个助手专注于一种"创建"类型：

```
Agent Architect      →  创建 "行为"
                         (如何工作、如何决策)

CLAUDE.md Creation   →  创建 "记忆"
                         (项目信息、规范约定)

Status Line Setup    →  创建 "界面"
                         (显示什么、如何显示)
```

### 2. 模板驱动设计

所有助手都使用模板结构：

```
┌─────────────────────────────────────┐
│         模板                        │
├─────────────────────────────────────┤
│  • 固定结构                         │
│  • 清晰章节                         │
│  • 必填字段                         │
│  • 可选字段                         │
└─────────────────────────────────────┘
         ↓
    填充具体内容
         ↓
┌─────────────────────────────────────┐
│         具体输出                    │
├─────────────────────────────────────┤
│  • 遵循模板                         │
│  • 包含具体信息                     │
│  • 可直接使用                       │
└─────────────────────────────────────┘
```

### 3. 渐进式细化

```
初步理解
    ↓
采集信息
    ↓
应用模板
    ↓
细化内容
    ↓
验证输出
```

---

*文档生成时间：2025年12月*
*基于 Claude Code v2.0.76*
